<%- include('./layouts/head.ejs') %>

    <body>
        <h1>Shop Page</h1>
        <% if(prods.length > 0){ %>
            <% prods.forEach(element => { %>
                <h4>First Name:
                    <%= element.fname %>
                </h4>
                <h4>Last Name:
                    <%= element.lname %>
                </h4>
                <% }) %>
                    <% }else{ %>
                        <h2>No Product Found</h2>
                        <% } %>
    </body>

    <%- include('./layouts/footer.ejs') %>